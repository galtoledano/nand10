import Tokenizer
import CompilationEngine
file = "test2.jack"

# t = Tokenizer.Tokenizer(file)
# f = CompilationEngine.CompilationEngine(file, "out")
# a = t.string_val()

print('"'.endswith('"'))
print('"'.startswith('"'))